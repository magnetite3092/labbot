import React from 'react';
import './Header.css'; // 必要に応じてスタイルを適用

const Header: React.FC = () => {
  return (
    <header className='App-header'>
      <h1>Todoリスト</h1>
    </header>
  );
};

export default Header;
